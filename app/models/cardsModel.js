/*
module.exports = (app, db) => (
	var cardSchema = new Schema ({
		artist: String,
		cardClass: String,
		required: String,
		collectible: Boolean,
		cost: Number,
		dbfId: Number,
		flavor: String,
		id: String,
		name: String,
		playRequirements: {
			REQ_MINION_TARGET: Number,
			REQ_TARGET_TO_PLAY: Number	
		},
		playerClass: String,
		rarity: String,
		'set': String,
		text: String,
		type: String
	});
);
*/